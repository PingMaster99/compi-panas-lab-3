class Main {
    a: Int;
    itr: Int;
    b: Int;
    my_bool: Bool <- 1;

    sum(a: Int, b: Int): Int {
        a + b
    };

    main() : Int {
        {
            a <- "RANDOM STRINGLOL";
            while itr < 20 + "w" loop
                if a < 10 then 
                    a <- 5+2 * 3
                else 
                    a <- 5*5 + 7
                fi 
            POOL;
            itr <-0;

            while my_bool = true loop
                self
            POOL;
            itr <-0;

        }
    };

    dude_function() : SELF_TYPE {
        {
            a <- 1;
            while a < 0 loop
             a <- 0
            POOL;
            self;
        }
    };
};